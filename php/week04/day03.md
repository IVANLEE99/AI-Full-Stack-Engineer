# Week 04 Day 03：配置中心概念

> 所属周：Week 04：配置中心 + 站点 API  
> 阶段：第一阶段：PHP + Yii2/TP 基础  
> 主仓库/项目：`mall-core`  
> 类型：编码练习  
> 建议时长：约 3h  
> 学习方法：PHP 后端主线 + JS/Node.js 类比 + AI Review

---

## 今日目标

理解远程配置、本地缓存和业务读取函数之间的关系，知道什么时候应该使用动态配置，什么时候应该使用静态配置或环境变量。

今天你要真正掌握这一句话：

> 配置中心的价值是把“需要运行时调整的业务参数”从代码中抽离出来，通过远程配置、本地缓存和默认值兜底，让系统既能动态调整，又能在配置服务异常时尽量稳定运行。

---

## 0. 今日学习路线

建议按下面顺序学习：

1. 先理解什么是配置中心
2. 区分远程配置、本地配置、环境变量
3. 理解动态配置和静态配置的适用场景
4. 理解为什么需要本地缓存
5. 画出“远程配置 → 本地缓存 → g_config() → 业务代码”的流程
6. 用 Nacos/Consul/etcd 做概念类比，不强行深入安装
7. 列出 5 个适合动态配置的业务场景
8. 列出 5 个不适合动态配置的场景
9. 完成配置类型判断练习

---

## 1. 学习内容

### 1.1 什么是配置中心？

配置中心可以理解成一个“集中管理配置的地方”。

以前配置可能写在代码里：

```php
<?php

$enableCoupon = true;
```

后来可能写在本地配置文件：

```php
<?php

return [
    'enable_coupon' => true,
];
```

再进一步，企业项目会把配置放到远程配置中心：

```text
配置中心
  - site.show_coupon = true
  - pay.enable_stripe = false
  - order.max_amount = 10000
```

业务代码通过统一函数读取：

```php
<?php

$enableCoupon = g_config('site', 'show_coupon', false);
```

小白重点：配置中心不是 PHP 独有概念，而是后端工程里常见的配置管理方案。

---

### 1.2 远程配置、本地配置、环境变量有什么区别？

| 类型 | 常见位置 | 适合存什么 | 是否适合频繁改 |
|---|---|---|---|
| 环境变量 | `.env` / 服务器环境 | DB 密码、Redis 地址、运行环境 | 不适合 |
| 本地配置文件 | `config/*.php` | 框架配置、服务配置 | 不适合频繁改 |
| 远程配置中心 | Nacos/Consul/etcd/DB | 业务开关、运营配置、阈值 | 适合 |
| 本地缓存 | 文件/内存/Redis | 远程配置副本 | 自动同步 |

举例：

```text
数据库密码：适合环境变量
是否展示优惠券入口：适合远程配置
路由规则：适合代码或框架配置
首页 banner：适合配置中心或后台配置
```

不要把所有东西都放配置中心。配置中心是工具，不是垃圾桶。

---

### 1.3 什么是动态配置？

动态配置是指：系统运行过程中，配置可以被调整，并影响业务行为。

例如：

```php
<?php

$showCoupon = g_config('site', 'show_coupon', false);
```

运营在后台把 `show_coupon` 从 `false` 改成 `true` 后，前端优惠券入口开始显示。

适合动态配置的场景：

| 场景 | 为什么适合动态配置 |
|---|---|
| 功能开关 | 可以快速开启/关闭 |
| 活动开关 | 活动开始结束需要调整 |
| 首页展示 | banner、导航经常变 |
| 风控阈值 | 需要按风险动态调整 |
| 支付渠道 | 某渠道异常时可临时关闭 |

---

### 1.4 什么是静态配置？

静态配置是指：不希望运行时随意变化，通常跟部署、框架、基础设施有关。

例如：

```env
APP_ENV=production
DB_HOST=127.0.0.1
REDIS_HOST=127.0.0.1
```

这些配置通常在部署时确定，不应该被运营后台随意修改。

适合静态配置的场景：

| 场景 | 为什么不适合动态配置 |
|---|---|
| 数据库连接 | 改错会导致系统不可用 |
| Redis 地址 | 属于基础设施配置 |
| APP_ENV | 决定运行环境 |
| 日志目录 | 和服务器部署相关 |
| Composer autoload | 属于代码结构 |

小白重点：动态配置偏业务，静态配置偏运行环境和基础设施。

---

### 1.5 为什么需要本地缓存？

如果每次调用 `g_config()` 都访问远程配置中心，会有问题：

- 请求慢
- 配置中心压力大
- 配置中心故障时业务也跟着故障
- 网络抖动会影响接口响应

所以常见做法是：

```text
远程配置中心
  ↓ 同步/拉取
本地缓存 / Redis / 文件缓存
  ↓ 快速读取
g_config()
  ↓
业务代码
```

本地缓存的作用：

| 作用 | 说明 |
|---|---|
| 提升性能 | 不必每次远程请求 |
| 降低依赖 | 配置中心短暂异常时仍可使用旧值 |
| 稳定接口 | 避免配置读取拖慢业务接口 |
| 支持兜底 | 没有缓存时再使用 default |

---

### 1.6 配置读取完整流程

你可以把完整流程画成：

```text
运营/管理员
  ↓ 修改配置
远程配置中心（如 Nacos/Consul/etcd/DB）
  ↓ 推送或拉取
本地缓存 / Redis / 文件缓存
  ↓
g_config(module, key, default)
  ↓
业务代码判断
  ↓
前端展示或业务行为变化
```

如果配置读取失败，兜底流程可能是：

```text
读本地缓存
  ↓ 如果没有
读远程配置
  ↓ 如果失败
返回 default
```

注意：真实项目里的顺序可能不同，你要以项目源码为准。

---

### 1.7 Nacos、Consul、etcd 怎么理解？

你今天不需要安装它们，只需要知道它们大概解决什么问题。

| 工具 | 可以类比成什么 | 常见用途 |
|---|---|---|
| Nacos | 配置中心 + 服务发现平台 | Java/微服务体系常见 |
| Consul | 服务发现 + KV 配置 | 微服务基础设施 |
| etcd | 分布式 KV 存储 | Kubernetes 等系统常见 |

对 PHP 小白来说，先记住：

```text
它们都可以作为“远程配置存储/分发”的基础设施。
```

不要一开始就陷入安装和集群原理，先理解“为什么需要远程配置”。

---

### 1.8 什么时候不要用动态配置？

动态配置很方便，但不是越多越好。

不适合动态配置的场景：

| 场景 | 风险 |
|---|---|
| 数据库表结构 | 改错会导致代码和 DB 不匹配 |
| 核心金额公式 | 可能引发财务风险 |
| 权限规则核心逻辑 | 改错会越权 |
| 密钥明文配置 | 有泄露风险 |
| 复杂业务流程 | 配置过多会变成不可维护的规则引擎 |

判断原则：

```text
如果改错会造成资金、安全、数据一致性严重问题，就不要轻易做成普通动态配置。
```

---

## 2. 源码阅读

本日无指定源码阅读，重点完成练习与复盘。

如果你想结合项目加深理解，可以回看：

- `g_config()` 的实现位置
- `ConfigHelper` 常量定义
- 配置 API 返回字段
- 项目是否有缓存配置的代码

记录：

| 观察点 | 记录 |
|---|---|
| 项目配置来源 |  |
| 是否有本地缓存 |  |
| 是否有默认值兜底 |  |
| 哪些配置适合动态修改 |  |

---

## 3. 练习任务

### 练习 1：画配置流程图

画出：

```text
远程配置 → 本地缓存 → g_config() → 业务代码 → 前端展示
```

要求：每个节点写一句作用。

### 练习 2：列动态 vs 静态配置场景

| 配置 | 动态/静态 | 原因 |
|---|---|---|
| 首页 banner |  |  |
| 数据库密码 |  |  |
| 支付渠道开关 |  |  |
| Redis 地址 |  |  |
| 最大下单金额 |  |  |
| APP_ENV |  |  |

### 练习 3：设计兜底策略

假设配置中心不可用，你会怎么处理？

| 场景 | 兜底方案 |
|---|---|
| 首页 banner 读取失败 |  |
| 支付渠道配置读取失败 |  |
| 客服电话读取失败 |  |
| 风控阈值读取失败 |  |

---

## 4. JS/Node.js 类比

| 配置中心概念 | Node.js 类比 |
|---|---|
| Nacos/Consul/etcd | remote config service / feature flag platform |
| 本地缓存 | memory cache / Redis cache |
| `g_config()` | config client wrapper |
| default 兜底 | `config.get(key) ?? defaultValue` |
| 动态配置 | feature flags / remote settings |

---

## 5. AI Review 提问

```text
我正在学习配置中心概念。
我已经区分了远程配置、本地缓存、动态配置、静态配置，并画了配置读取流程。
请你检查：
1. 我对配置中心的理解是否正确？
2. 哪些配置我错误地归类为动态配置？
3. 配置中心不可用时，我的兜底策略是否合理？
4. Nacos/Consul/etcd 的类比是否准确？
5. 真实项目里配置中心最常见的风险是什么？
```

---

## 6. 今日产出

- [ ] 配置中心概念笔记
- [ ] 远程配置 → 本地缓存 → `g_config()` 流程图
- [ ] 动态 vs 静态配置场景表
- [ ] 配置失败兜底策略表
- [ ] AI Review 记录

---

## 7. 今日完成标准

- [ ] 能解释什么是配置中心
- [ ] 能区分远程配置、本地配置、环境变量
- [ ] 能说明为什么需要本地缓存
- [ ] 能列出 5 个适合动态配置的场景
- [ ] 能列出 3 个不适合动态配置的场景
- [ ] 能画出配置读取流程
- [ ] 能说明配置中心故障时如何兜底

---

## 8. 学习记录

| 记录项 | 内容 |
|--------|------|
| 今日最清楚的概念 |  |
| 今日最卡的概念 |  |
| JS/Node 类比是否帮助理解 |  |
| 实际耗时 |  |
| 明日要补的问题 |  |

---

## 9. AI Review 提示词

```text
我正在进行 Week 04 Day 03：配置中心概念 的学习。
请你扮演资深 PHP 后端工程师，帮我检查：
1. 今日理解是否正确
2. JS/Node 类比是否准确
3. 练习任务是否遗漏关键风险
4. 真实企业项目中还需要注意什么

请用中文输出：问题清单、修正建议、下一步练习。
```

---

## 返回本周

- [返回 Week 04 README](./README.md)
